{
  "mcpServers": {
    "salesforce-dx": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@salesforce/mcp",
        "--orgs",
        "coastline",
        "--toolsets",
        "data,metadata,testing",
        "--no-telemetry"
      ]
    }
  }
}
